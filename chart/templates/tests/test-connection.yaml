apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cryptopro-rest.fullname" . }}-test-connection"
  labels:
    {{- include "cryptopro-rest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cryptopro-rest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
